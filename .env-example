# Shell-compatible env file (meant to be sourced).
#
# Usage:
#   cp .env.example .env
#   ./scripts/audio-transcribe --help
#
# Notes:
# - Use absolute paths (or ${PWD}/...) so it works regardless of your login shell.
# - This file is *shell* syntax (supports ${VAR} / ${VAR:+...} expansions).

# Where to store NLTK tokenizer data for WhisperX alignment.
NLTK_DATA=/path/to/nltk_data

# Virtualenv root (project uses uv + local .venv by default).
VENV="${PWD}/.venv"

# Ensure CUDA-related shared libs are discoverable at runtime.
# Avoid adding an empty path element (which would imply current directory) when LD_LIBRARY_PATH is empty/unset.
LD_LIBRARY_PATH="${VENV}/lib/python3.13/site-packages/nvidia/cudnn/lib:${VENV}/lib/python3.13/site-packages/torch/lib${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}"

# 使用 --diarize 时模型需要授权下载
HF_TOKEN=...
